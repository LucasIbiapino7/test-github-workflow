name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, linux, x64]

    steps:
      - name: Deploy local
        run: |
            cd /opt/wanda-cicd-test
            git fetch origin main
            git reset --hard origin/main
            docker compose up --build -d
